- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc: zsh/.zshrc
    ~/.oh-my-zsh/custom/aliases.zsh: zsh/.aliases

- shell:
    - [git submodule update --init --recursive, Installing submodules]
